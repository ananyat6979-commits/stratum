//! `stratum-raft` — reserved workspace member. **Not implemented.**
//!
//! Planned scope (deferred, not started): custom Raft consensus for
//! STRATUM's configuration plane (routing weights, experiment
//! parameters, chaos schedules) — not the serving path itself.
//!
//! This crate was previously scaffolded with named-but-empty submodule
//! files (`node.rs`, `election.rs`, `log.rs`, etc.) implying partial
//! progress that did not exist. Those were removed; see the repo's
//! `docs/SCOPE.md` for the full accounting of what's real vs. deferred
//! and why an empty, honest crate is preferred over misleading
//! placeholder files.
