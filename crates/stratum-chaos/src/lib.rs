//! `stratum-chaos` — reserved workspace member. **Not implemented.**
//!
//! Planned scope (deferred, not started): fault injection for a small
//! subset of the originally-designed failure-mode taxonomy that is
//! actually achievable against the project's real backend (process
//! kill / partition simulation), not the full backend-internal taxonomy
//! (KV eviction storms, attention OOM) which requires inference-server
//! hooks the project does not have. See `docs/SCOPE.md`.
//!
//! Previously scaffolded with 8 named-but-empty taxonomy injector files
//! implying partial progress that did not exist; those were removed.
