//! `stratum-scheduler` — reserved workspace member. **Not implemented.**
//!
//! Planned scope (deferred, not started): a NUMA-aware, predicted-length
//! scheduler intended to replace an inference backend's default FCFS
//! scheduling. This requires backend-internal scheduling hooks that the
//! project's actual inference backend (Ollama) does not expose — see
//! `docs/SCOPE.md` for why this phase is blocked on backend choice, not
//! merely "not yet started."
//!
//! Previously scaffolded with named-but-empty submodule files implying
//! partial progress that did not exist; those were removed.
